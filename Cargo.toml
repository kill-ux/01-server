[package]
name = "localserver"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
mio = { version = "1.0.4", features = ["os-poll", "net"] }