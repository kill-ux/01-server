[package]
name = "server_proxy"
version = "0.1.0"
edition = "2024"

[dependencies]
mio = { version = "1.1.1", features = ["os-poll", "net"] }
parser = { path = "parser"}


[workspace]
members = ["parser"]

resolver = "2"
